# my-blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.
`content/posts/*.md`를 넣고 빌드하면 `dist/`에 배포 가능한 정적 사이트가 나온다.

## 핵심 제약 (하드 룰)

이 제약은 협의 없이 깨지 않는다. 편의를 위해 어기고 싶어지는 순간이 반드시 오는데, 그때 사용자에게 먼저 묻는다.

- **프레임워크 금지.** React/Vue/Svelte/Next/Astro/11ty/Jekyll 등 일절 사용하지 않는다.
- **런타임 의존성 0개.** 브라우저로 내려가는 코드에 외부 라이브러리·CDN 스크립트·웹폰트 요청이 없어야 한다.
- **npm 패키지 0개.** 빌드 스크립트도 Node 표준 라이브러리(`node:fs`, `node:path` 등)만 쓴다. `package.json`은 스크립트 정의 용도로만 두고 `dependencies`는 비운다.
- **빌드 결과물은 순수 정적 파일.** 서버 런타임 없이 아무 정적 호스팅에나 올라가야 한다.
- **CSS 전처리기 금지.** 순수 CSS + CSS 커스텀 프로퍼티만 쓴다.
- **빌드 산출물은 손으로 고치지 않는다.** `dist/`를 수정해야 할 것 같으면 그건 템플릿이나 빌드 스크립트를 고쳐야 한다는 신호다.

## 디렉터리 구조

```
site.config.mjs      # 사이트 제목·설명·저자·배포 URL — 배포 전 고칠 유일한 설정
content/
  posts/             # 글 원본 (.md) — 파일명이 곧 slug
  pages/             # 정적 페이지 (about.md 등)
src/
  build.mjs          # 빌드 엔트리 + 프런트매터 파싱
  markdown.mjs       # 직접 구현한 마크다운 파서
  markdown.test.mjs  # 파서 회귀 테스트
  template.mjs       # HTML 셸/레이아웃 생성
  serve.mjs          # 로컬 미리보기 서버 (개발용, 배포에는 안 씀)
static/
  styles.css         # 전체 스타일 (단일 파일)
  theme.js           # 다크 모드 토글
scripts/             # 로컬 자동화. 사이트 빌드와 무관하고 dist/에 들어가지 않는다
  study-check.mjs    # STUDY 회차 점검 — 확인 전용, 파일을 만들지 않는다
  study-notify.ps1   # Windows 예약 작업 래퍼 — 위 스크립트 실행 후 알림 팝업
  import-obsidian.mjs # 옵시디언 볼트 → content/posts. 볼트는 읽기만 한다
dist/                # 빌드 산출물 — git 추적 안 함, 손으로 수정 금지
.github/workflows/
  deploy.yml         # master에 푸시하면 깃허브가 빌드해 GitHub Pages에 올린다
```

## 빌드 파이프라인

`node src/build.mjs`가 하는 일:

1. `dist/`를 비운다.
2. `content/**/*.md`를 읽어 프런트매터와 본문을 분리한다.
3. 본문을 `markdown.mjs`로 HTML로 변환한다.
4. `template.mjs`로 감싸 `dist/posts/<slug>/index.html`로 쓴다. (확장자 없는 깔끔한 URL)
5. 글 목록으로 `dist/index.html`을 만든다. 날짜 내림차순.
6. `static/`을 `dist/`로 복사한다.
7. `dist/rss.xml`, `dist/sitemap.xml`을 생성한다.

빌드는 **결정적(deterministic)** 이어야 한다. 같은 입력 → 같은 출력. 빌드 시각 같은 걸 산출물에 박지 않는다.

## 프런트매터

`---`로 감싼 단순 `key: value` 형식. YAML 파서를 만들지 말고 필요한 키만 파싱한다.

```markdown
---
title: 글 제목
date: 2026-08-10
tags: 개발, 회고
draft: false
description: 목록과 og:description에 쓰일 한 줄 요약
---
```

- `title`, `date`는 필수. 없으면 빌드를 **실패**시킨다 (조용히 넘어가지 않는다).
- `draft: true`인 글은 빌드에서 제외한다.
- 값에 콜론이 들어갈 수 있으므로 첫 번째 `:`에서만 분리한다.

## 마크다운 파서 (`src/markdown.mjs`)

CommonMark 완전 준수가 목표가 아니다. **블로그 글을 쓰는 데 실제로 필요한 문법**만 지원한다.

지원 범위: 제목(h1–h4), 문단, `**굵게**`/`*기울임*`/`` `인라인 코드` ``, 순서·비순서 목록(중첩 포함), 펜스 코드 블록(``` + 언어 표시), 인용문, 링크, 이미지, 수평선, 표.

구현 규칙:

- **블록 파싱 먼저, 인라인 파싱 나중.** 줄 단위로 블록을 나눈 뒤 각 블록 안에서 인라인을 처리한다. 정규식 하나로 전부 처리하려 들지 않는다.
- **HTML 이스케이프는 필수.** `&`, `<`, `>`, `"`를 이스케이프한다. 특히 코드 블록 내부는 어떤 인라인 변환도 적용하지 않고 이스케이프만 한다.
- **원문 HTML 통과(raw HTML passthrough)는 지원하지 않는다.** 콘텐츠는 신뢰할 수 있는 로컬 파일이지만, 지원하는 순간 이스케이프 규칙이 무너진다.
- 코드 블록 언어는 `<code class="language-js">`로 표기만 하고 **하이라이팅은 하지 않는다.** (하이라이터는 사실상 런타임 의존성이다.)
- 문법을 추가할 때는 `src/markdown.test.mjs`에 케이스를 먼저 추가한다. `node --test`로 돌린다.

## 디자인

목표는 "깔끔하고 읽기 좋게". 장식보다 가독성이 우선이다.

- **본문 폭은 65–75자.** `max-width: 68ch`. 이 값을 넓히자는 유혹을 거절한다.
- **본문 행간 1.7 이상**, 본문 크기 `1.0625rem`(모바일) → `1.125rem`(데스크톱).
- **시스템 폰트 스택만 사용.** 웹폰트를 불러오지 않는다(네트워크 요청 0 규칙).
- **수직 리듬은 하나의 간격 스케일**로 통일: `--space-1` ~ `--space-6` (0.25 / 0.5 / 1 / 1.5 / 2.5 / 4rem).
- 색은 무채색 위주 + 링크/강조용 액센트 **한 가지**. 색상값은 반드시 CSS 변수로만 참조하고, 규칙 안에 하드코딩한 hex를 남기지 않는다.
- 애니메이션은 최소한으로. 넣더라도 `@media (prefers-reduced-motion: reduce)`에서 끈다.

## 다크 모드

3-상태 모델: `system`(기본) / `light` / `dark`.

- 색은 전부 `:root`의 CSS 변수로 정의하고, 다크 값은 `@media (prefers-color-scheme: dark)`와 `:root[data-theme="dark"]` 양쪽에서 덮어쓴다. 사용자가 명시적으로 고른 테마(`data-theme`)가 시스템 설정을 항상 이긴다.
- 선택값은 `localStorage`에 저장한다.
- **FOUC 방지가 핵심.** `<head>` 안에 저장된 테마를 읽어 `<html>`에 `data-theme`을 다는 **인라인 동기 스크립트**를 둔다. 이 스크립트는 외부 파일로 빼면 안 된다(파일 로드 전에 첫 페인트가 일어난다).
- 다크 모드에서 순수 검정(`#000`) 배경과 순수 흰색(`#fff`) 본문 텍스트를 쓰지 않는다. 눈부심과 헤일로가 생긴다.
- `<meta name="color-scheme" content="light dark">`를 넣어 스크롤바·폼 컨트롤도 따라오게 한다.

## 모바일 / 반응형

- **모바일 퍼스트로 작성한다.** 기본 스타일이 모바일이고, `min-width` 미디어 쿼리로 넓은 화면을 얹는다.
- 브레이크포인트는 두 개면 충분하다: `48rem`(태블릿), `64rem`(데스크톱). 기기 이름이 아니라 레이아웃이 깨지는 지점을 기준으로 정한다.
- 좌우 스크롤은 어떤 폭에서도 생기면 안 된다. 표·코드 블록은 각자 `overflow-x: auto` 컨테이너 안에서 스크롤한다.
- 터치 타깃 최소 `44x44px`. 테마 토글 버튼이 특히 걸리기 쉽다.
- 이미지는 `max-width: 100%; height: auto`. 레이아웃 시프트를 막기 위해 빌드 시 `width`/`height` 속성을 넣을 수 있으면 넣는다.
- 고정 헤더를 쓴다면 모바일에서 화면 높이를 잡아먹지 않는지 확인한다. 기본은 고정하지 않는 쪽.

## 접근성

- 시맨틱 태그를 쓴다. `<article>`, `<nav>`, `<main>`, `<time datetime="...">`. `<div>`로 버튼을 만들지 않는다.
- 본문 대비비 4.5:1 이상 — 라이트/다크 **양쪽에서** 확인한다.
- 키보드 포커스 링을 지우지 않는다. 바꾸려면 `:focus-visible`로 더 잘 보이게 바꾼다.
- 테마 토글에 `aria-label`과 현재 상태를 넣는다.
- 이미지 `alt`는 마크다운에서 받아 그대로 넘긴다. 빈 alt는 장식용일 때만.

## 코딩 컨벤션

- **JavaScript**: ES 모듈(`.mjs`), 세미콜론 사용, 2칸 들여쓰기. 트랜스파일 없으므로 최신 문법을 그냥 쓴다.
- **CSS**: 클래스명은 소문자 케밥 케이스. 중첩 셀렉터는 2단계까지. ID 셀렉터와 `!important`는 쓰지 않는다.
- 주석은 "무엇"이 아니라 "왜"를 적는다. 파서의 비직관적인 분기처럼 이유가 필요한 곳에만.
- 빌드 실패는 시끄럽게. 문제를 감지하면 부분 산출물을 남기지 말고 명확한 메시지와 함께 종료한다.

## 명령어

```bash
node src/build.mjs
```

초안까지 포함해 화면을 확인할 때. **배포용이 아니다** — 초안에는 "초안" 배지와 안내가 붙고,
RSS·사이트맵에는 어느 모드에서도 초안이 들어가지 않는다.

```bash
node src/build.mjs --drafts
```

```bash
node --test "src/*.test.mjs"
```

로컬 미리보기 (`dist/`를 정적 서버로 띄운다 — 깔끔한 URL 때문에 `file://`로 열면 안 된다):

```bash
node src/serve.mjs
```

`node`가 PATH에 없으면 `"C:\Program Files\nodejs\node.exe"` 전체 경로로 호출한다.

## 배포

`https://asdfdsign.github.io/my-blog/` — GitHub Pages. `master`에 **푸시하면** 배포된다.
커밋만 하고 푸시하지 않으면 사이트는 옛날 상태 그대로다.

```
파일 수정 → git commit  (내 PC)  →  git push  (여기서부터 사이트에 반영)
```

배포는 `.github/workflows/deploy.yml`이 한다. 깃허브 서버에서 테스트를 돌리고, 통과하면
`node src/build.mjs`로 구운 `dist/`를 Pages에 올린다. **테스트가 깨지면 배포되지 않는다.**
`--drafts`를 붙이지 않으므로 초안은 어떤 경로로도 배포되지 않는다.

### base — 하위 경로 배포

사이트가 도메인 루트가 아니라 `/my-blog/` 아래에 있다. 그래서 `/styles.css` 같은 루트 절대
경로를 그대로 쓰면 도메인 최상단을 가리켜 **전부 404가 난다.**

`site.config.mjs`의 `url`에서 경로 부분을 뽑아 `base`를 만들고, 내부 링크에 전부 붙인다.
고칠 자리는 `url` **하나뿐이다.** 루트 도메인으로 옮기면 `base`는 자동으로 빈 문자열이 되고
코드는 그대로 동작한다.

- 템플릿의 내부 링크는 전부 `href()` 헬퍼를 거친다. `href="/..."`를 직접 쓰지 않는다
- 본문 마크다운의 `/`로 시작하는 링크·이미지는 파서가 `base`를 붙여준다. 글에는 배포 위치를
  적지 않는다
- 외부 URL(`https://`)과 프로토콜 상대 URL(`//`)은 건드리지 않는다
- `serve.mjs`도 `base`를 벗겨내고 서빙한다. **로컬 미리보기 주소가 배포 주소와 같아야** 미리보기가
  확인 수단으로 의미가 있다 — `http://localhost:8080/my-blog/`

이 규칙은 **빌드가 강제한다.** 위반을 문서로만 막으면 언젠가 깨지는데, 증상이 "CSS가 조용히
사라짐"이라 알아차리기도 어렵다. 그래서 빌드가 `dist/`를 건드리기 전에 생성될 HTML과
`static/`의 CSS를 스스로 훑어, base 없는 내부 절대 경로가 하나라도 있으면 **어느 파일의 어느
경로인지 짚어서 실패한다.** 깃허브 Actions도 같은 빌드를 돌리므로 잘못된 링크는 배포되지 않는다.

`.js`는 검사하지 않는다. 문자열 안의 경로를 정적으로 가려낼 방법이 마땅치 않아 오탐이 는다.
브라우저로 내려가는 JS에서 경로를 다루게 되면 그때 다시 판단한다.

## 새 글 추가하기

1. `content/posts/<slug>.md` 생성 — 파일명이 URL이 되므로 소문자 하이픈 케밥 케이스로.
2. 프런트매터에 최소한 `title`, `date` 작성.
3. 빌드 후 브라우저에서 확인. 라이트/다크, 좁은 폭/넓은 폭 **네 조합**을 다 본다.

## 옵시디언에서 가져오기

`scripts/import-obsidian.mjs`가 볼트의 글을 `content/posts/`로 옮긴다. **볼트는 읽기만 한다.**

```bash
node scripts/import-obsidian.mjs --list
node scripts/import-obsidian.mjs "글 제목" --slug english-slug --dry-run
```

- 제목이 ASCII가 아니면 `--slug`를 **반드시** 받는다. 한글 주소는 퍼센트 인코딩으로 깨져 보인다
- 태그만 있는 줄(`#정리중`)은 프런트매터로 올린다. 본문에 두면 글자로 찍힌다
- `정리중`·`draft`·`wip`·`초안` 태그가 있으면 자동으로 `draft: true`
- 위키링크 `[[대상|표시]]`는 파서가 모르므로 일반 텍스트로 바꾼다
- 이미지 임베드 `![[...]]`는 **자동 변환하지 않고 경고만 한다** — 파일도 같이 옮겨야 해서 사람이 판단할 일이다
- 문장 중간에 섞인 태그는 건드리지 않는다. 지우면 글이 바뀐다

본문 글자는 고치지 않는다. 오타든 메모든 원문 그대로 옮기고, 고칠지는 사용자가 정한다.

## 시리즈

같은 제목의 글이 목록에 여러 줄 반복되지 않게, 프런트매터의 `series`로 묶는다.

```markdown
series: claude-code-study
```

- `series` 값은 **주소가 되므로** 소문자·숫자·하이픈만 쓴다. 어기면 빌드가 실패한다
- 목록 페이지에는 시리즈가 **한 줄**로 나오고, 누르면 `/series/<값>/`에서 회차를 본다
- 회차 이름은 **본문의 첫 `##` 소제목**에서 가져온다. 따로 적지 않는다
- 회차는 **오래된 것부터** 보여준다. 연재는 쌓인 순서로 읽는 게 자연스럽다
- 시리즈 표시 이름은 소속 글들의 `title`을 쓴다. 그래서 한 시리즈의 글은 `title`이 같아야 한다
- 시리즈에 속하지 않은 글은 지금처럼 목록에 그대로 나온다

## "…에 관해" 시리즈

한 주제를 두고 쌓아 올린 단상 모음. STUDY와 같은 구조를 쓴다 — 하루치가 아니라 주제 하나가
글 하나다.

- 파일: `content/posts/about-<주제>.md` (영문 슬러그)
- `title`: `…에 관해`
- 본문 첫 줄은 소제목 `## <주제>에 관해` — 그 아래 내용은 `###`부터 쓴다
- STUDY와 달리 회차 번호를 붙이지 않는다. 순서보다 주제가 식별자다

## STUDY 시리즈

학습 기록을 매일 한 편씩 쌓는 연재. 하루에 글 하나이며, 기존 글에 덧붙이지 않는다.

- 파일: `content/posts/claude-code-study-<N>.md` (1회차만 번호 없이 `claude-code-study.md`)
- `title`: `클로드 코드 STUDY`
- 본문 첫 줄은 소제목 `## STUDY<N>_<YYMMDD>` — 그 아래 내용은 `###`부터 쓴다
- `<N>`은 회차 번호로 1씩 증가한다. 날짜와 무관하게 순서를 센다
- `<YYMMDD>`는 그 글이 다루는 날짜

작성 시점은 **오후 6시**를 기준으로 삼고, 두 겹으로 챙긴다.

**1단 — 세션을 시작할 때 확인하고 묻는다. 이쪽이 항상 동작하는 경로다.**
`content/posts/claude-code-study*.md`에서 마지막 회차 번호와 날짜를 읽고, 오늘까지 빠진 날이
있으면 사용자가 묻기 전에 알린다. PC가 며칠 꺼져 있었을 수 있으므로 며칠치가 밀렸는지 함께
알린다. 그다음 **지금 쓸지 물어보고, 답을 받은 뒤에 작성한다.** 묻지 않고 먼저 쓰지 않는다 —
그날 무엇을 배웠는지는 사용자만 안다. `draft: true`인 글은 아직 안 끝난 것으로 센다.

**2단 — Windows 예약 작업 `my-blog-study-daily`.** 매일 18:00에 `scripts/study-notify.ps1`을
실행한다. PC가 꺼져 있었으면 다음 부팅 후에 실행된다(`StartWhenAvailable`).
**확인과 알림만 한다. 파일을 만들거나 고치지 않는다.** 빈 초안을 미리 깔아두면 자리를 비운
날수만큼 내용 없는 파일이 쌓이고 그걸 다시 치워야 한다. 글을 쓰는 일은 전부 1단의 대화로
넘긴다. 2단은 "오늘 안 썼다"고 화면에 띄우는 역할까지다.

예약 작업이 없어도 1단은 그대로 동작한다. 없애려면:
`Unregister-ScheduledTask -TaskName 'my-blog-study-daily'`

`study-notify.ps1`은 **ASCII로만 쓴다.** Windows PowerShell 5.1은 BOM 없는 `.ps1`을 ANSI로
읽어서 한글이 깨진다. 어느 PowerShell이 작업을 실행할지 보장할 수 없으므로, 한글 문구는 전부
Node 쪽 stdout에서 오게 하고 PS 쪽은 UTF-8로 디코딩만 한다.

## 환경 메모

- 개발 환경은 Windows + PowerShell. 셸 명령을 안내할 때 POSIX 전용 문법을 가정하지 않는다.
- 경로 조작은 반드시 `node:path`로. 문자열 `/` 연결로 경로를 만들지 않는다.
- 파일은 UTF-8로 읽고 쓴다. 한글 콘텐츠가 깨지는 대부분의 원인이 여기다.
